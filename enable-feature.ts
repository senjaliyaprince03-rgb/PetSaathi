import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { await prisma.featureFlag.upsert({ where: { key: "live_walk_tracking" }, update: { enabled: true }, create: { key: "live_walk_tracking", enabled: true, description: "Phase 6: Live Walk Tracking" } }); console.log("Enabled live_walk_tracking"); } main().catch(console.error).finally(() => prisma.$disconnect());
