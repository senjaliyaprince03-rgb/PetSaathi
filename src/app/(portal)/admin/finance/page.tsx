import { IndianRupee, Landmark, RotateCcw } from "lucide-react";
import { redirect } from "next/navigation";

import { PayoutActions, ReconciliationPanel, RefundActions } from "@/components/portal/finance-actions";
import { prisma } from "@/lib/db";
import { getCurrentIdentity, hasAnyRole } from "@/modules/auth/session";

export const dynamic = "force-dynamic";

export default async function AdminFinancePage() {
  const identity = await getCurrentIdentity();
  if (!identity || !hasAnyRole(identity, ["FINANCE_ADMIN", "SUPER_ADMIN"])) redirect("/login?returnTo=/admin/finance");
  const [refunds, payouts, reconciliationRows] = await Promise.all([
    prisma.refund.findMany({ where: { status: { notIn: ["COMPLETED", "REJECTED"] } }, orderBy: { createdAt: "asc" }, take: 50, select: { id: true, amountPaise: true, reason: true, status: true, providerRefundId: true, createdAt: true, payment: { select: { booking: { select: { reference: true } } } } } }),
    prisma.payout.findMany({ where: { status: { notIn: ["PAID", "CANCELLED"] } }, orderBy: { createdAt: "asc" }, take: 50, select: { id: true, amountPaise: true, adjustmentPaise: true, status: true, providerRef: true, createdAt: true, booking: { select: { reference: true, status: true, reports: { orderBy: { version: "desc" }, take: 1, select: { reviewStatus: true } } } }, sitter: { select: { user: { select: { displayName: true } } } } } }),
    prisma.reconciliationRun.findMany({ orderBy: { createdAt: "desc" }, take: 50, select: { id: true, provider: true, periodStart: true, periodEnd: true, status: true, expected: true, actual: true, differences: true } })
  ]);
  const total = (value: unknown) => value && typeof value === "object" ? value as { capturedPaise: number; refundedPaise: number; paidOutPaise: number; netCashPaise: number } : null;
  const reconciliationRuns = reconciliationRows.map((run) => ({ ...run, status: String(run.status), periodStart: run.periodStart.toISOString(), periodEnd: run.periodEnd.toISOString(), expected: total(run.expected), actual: total(run.actual), differences: total(run.differences) }));
  return <main className="min-h-screen bg-cream/50 py-10"><div className="container-shell"><p className="eyebrow">finance controls</p><h1 className="section-title mt-5">Refunds, payouts and reconciliation</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-ink/60">Provider calls, approvals, bank references and statement comparisons remain separate so money cannot skip a review state.</p><div className="mt-10 grid gap-8 xl:grid-cols-2"><section><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-coral/10 text-coral"><RotateCcw className="h-5 w-5" /></span><div><h2 className="font-display text-3xl font-semibold">Refund queue</h2><p className="text-sm text-ink/45">{refunds.length} open request{refunds.length === 1 ? "" : "s"}</p></div></div><div className="mt-5 grid gap-4">{refunds.length ? refunds.map((refund) => <article key={refund.id} className="rounded-4xl border border-ink/10 bg-paper p-5 shadow-lifted"><div className="flex items-center justify-between gap-3"><p className="text-xs font-bold uppercase tracking-[0.15em] text-ink/45">{refund.payment.booking.reference}</p><span className="rounded-full bg-coral/10 px-3 py-1 text-xs font-bold text-coral">{refund.status}</span></div><p className="mt-4 flex items-center font-display text-3xl font-semibold"><IndianRupee className="h-5 w-5" />{(refund.amountPaise / 100).toLocaleString("en-IN")}</p><p className="mt-3 text-sm leading-6 text-ink/55">{refund.reason}</p>{refund.providerRefundId && <p className="mt-2 text-xs text-ink/40">Provider: {refund.providerRefundId}</p>}<RefundActions id={refund.id} status={refund.status} /></article>) : <EmptyState label="No refunds need review." />}</div></section><section><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-leaf/10 text-leaf"><Landmark className="h-5 w-5" /></span><div><h2 className="font-display text-3xl font-semibold">Payout queue</h2><p className="text-sm text-ink/45">{payouts.length} open payout{payouts.length === 1 ? "" : "s"}</p></div></div><div className="mt-5 grid gap-4">{payouts.length ? payouts.map((payout) => { const reviewReady = payout.booking.status === "CLOSED" && payout.booking.reports[0]?.reviewStatus === "APPROVED"; return <article key={payout.id} className="rounded-4xl border border-ink/10 bg-paper p-5 shadow-lifted"><div className="flex items-center justify-between gap-3"><p className="text-xs font-bold uppercase tracking-[0.15em] text-ink/45">{payout.booking.reference} · {payout.sitter.user.displayName}</p><span className="rounded-full bg-leaf/10 px-3 py-1 text-xs font-bold text-leaf">{payout.status}</span></div><p className="mt-4 flex items-center font-display text-3xl font-semibold"><IndianRupee className="h-5 w-5" />{((payout.amountPaise + payout.adjustmentPaise) / 100).toLocaleString("en-IN")}</p>{payout.providerRef && <p className="mt-2 text-xs text-ink/40">Transfer: {payout.providerRef}</p>}<PayoutActions id={payout.id} status={payout.status} reviewReady={reviewReady} /></article>; }) : <EmptyState label="No payouts need review." />}</div></section></div><ReconciliationPanel runs={reconciliationRuns} /></div></main>;
}

function EmptyState({ label }: { label: string }) { return <div className="rounded-4xl border border-dashed border-ink/15 bg-paper/60 p-8 text-center text-sm font-semibold text-ink/45">{label}</div>; }
